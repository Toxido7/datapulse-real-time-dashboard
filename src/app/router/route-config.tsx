import type { AppNavigationItem } from "@/lib/constants";
import { NAVIGATION_ITEMS } from "@/lib/constants";

export type AppRouteMeta = AppNavigationItem;

export const appRoutes: AppRouteMeta[] = NAVIGATION_ITEMS;
